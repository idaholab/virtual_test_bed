---
description: Find a Virtual Test Bed (VTB) reactor model
---

Use the `vtb-docs` skill to find a Virtual Test Bed model matching: $ARGUMENTS

Follow the skill's instructions in `skills/vtb-docs/SKILL.md` — check
`references/model-index.json` first, surface the model's `doc_url`, and
note its `open_source_tier` if relevant.
